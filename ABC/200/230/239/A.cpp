#include<bits/stdc++.h>
#define REP(i, n) for (int i = 0; i < (int)(n); i++)
using ll = long long;
using namespace std;
int main() {
  long double x;
  cin >> x;
  cout << fixed << setprecision(7) << sqrt(x * (12800000 + x)) << endl;
}