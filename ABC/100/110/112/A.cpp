#include <bits/stdc++.h>
#define REP(i, n) for (int i = 0; i < (int)(n); i++)
using ll = long long;
using namespace std;
int main() {
  int N;
  cin >> N;
  if (N == 1) {
    cout << "Hello World\n";
  } else {
    int a, b;
    cin >> a >> b;
    cout << a + b << endl;
  }
}