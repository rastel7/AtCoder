#include <bits/stdc++.h>
using namespace std;
#define REP(i, n) for(ll i = 0; i < (ll)(n); i++)
typedef long long ll;
typedef pair<ll, ll> P;
const ll INF = 1e18;
const ll MOD = 1000000007;

int main() {
    int T;
    cin >> T;
    while(T--) {
        int N, A, B;
        cin >> N >> A >> B;
        map<int, int> mp;
        REP(i, N) {
            int a;
            cin >> a;
            mp[a]++;
        }
        ll ans = 1;

    }
}