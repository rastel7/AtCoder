#include <bits/stdc++.h>
using namespace std;
#define REP(i, n) for(ll i = 0; i < (ll)(n); i++)
#define OUT(V)                                                                 \
    for(int i = 0; i < (int)V.size(); ++i) {                                   \
        cout << V[i] << " ";                                                   \
    }                                                                          \
    cout << endl;
typedef long long ll;
typedef pair<ll, ll> P;
const ll INF = 1e18;
const ll MOD = 1000000007;
const ll MAX = 360 * 12 * 1e10;
int main() {
    int T;
    cin >> T;
    while(T--) {
        ll a, b, c;
        cin >> a >> b >> c;
        ll mod = 720 - c % 720;
        ll h, m, s, n;
        h = 0;
        
    }
}