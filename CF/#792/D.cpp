#include <bits/stdc++.h>
#define REP(i, n) for(int i = 0; i < (int)(n); i++)
using ll = long long;
using namespace std;
vector<ll> ans;
void solve() {
    ll n, k;
    cin >> n >> k;
    vector<ll> a(n);
    REP(i, n) cin >> a[i];
    
}
int main() {
    int t;
    cin >> t;
    while(t--) {
        solve();
    }
}